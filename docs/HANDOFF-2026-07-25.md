# Handoff — Toro Movers CRM & stage SMS (2026-07-25)

**Status:** Save point. Pipeline CRM parked; HubSpot + OpenPhone + Resend stage SMS live on site; n8n delayed drip needs one manual activate in Railway n8n.

---

## Goal of next session

Pick up where we left off:

1. **Confirm n8n workflow is Active** (import if not done) — delayed SMS 1h/24h/72h.
2. Optionally: wire **website leads → crm.toromovers.net** Pipeline board.
3. Optionally: polish Pipeline UI / claim durable Postgres for CRM.

---

## State of play

### Done

| Area | What | Where |
|------|------|--------|
| **Pipeline CRM (parked)** | Jobs-style board, stages, auth, stage automations, SMS via site proxy | `~/toro-pipeline` · https://crm.toromovers.net |
| **Review link** | Google review URL set | `https://g.page/r/CYAKurQHh5TvEAI/review` |
| **Meta ads QA** | Fixed TOROMOOVERS typo pack + 9:16 safe crops | `~/Downloads/TORO-DILER/exports-fixed/` · age targeting 35–65 applied via API |
| **HubSpot stage SMS plan** | Multi-SMS per stage (instant + delays defined) | `src/lib/crm/stage-sms-plan.ts` |
| **Instant send** | On new lead + stage advance → OpenPhone/Resend | `intake.ts`, `stage.ts`, live on toromovers.net |
| **Delayed API** | `POST /api/crm/sequences/stage-run` | Live |
| **Plan API** | `GET /api/crm/sequences/plan` | Live |
| **SMS proxy** | Pipeline → site OpenPhone | `POST /api/crm/send` |
| **n8n workflow file** | Importable drip | `docs/n8n-stage-sms-drip.workflow.json` |
| **Netlify env** | `N8N_CRM_WEBHOOK_URL` + `N8N_STAGE_WEBHOOK_URL` → `…/webhook/toro-stage-sms-drip` | live-toro-site |

### Blocked / manual

| Item | Action |
|------|--------|
| **n8n Activate** | User must import + toggle Active on Railway n8n (cannot do from agent) |
| **Pipeline Postgres** | Claim Prisma create-db or move to Neon — ephemeral DB may expire |
| **GitHub → Netlify CI for pipeline** | Private repo access was flaky; deploys often via `netlify deploy --build --prod` |

### SMS counts (HubSpot stages)

| Stage | SMS | Timing |
|-------|----:|--------|
| New Lead | 4 | 0 · 1h · 24h · 72h |
| No Answer | 3 | 0 · 4h · 24h |
| Contacted | 2 | 0 · 24h |
| Quote Sent | 3 | 0 · 24h · 48h |
| Booked | 2 | 0 · eve |
| Move Done | 1 | review link |
| Review Sent | 1 | nudge |

Without n8n Active: **only instant (0 min) SMS** fire.

---

## Open decisions

1. Deactivate old n8n “CRM follow-up drip (followup_1h/24h/72h)” if it double-fires with new stage drip?
2. Auto-push every website lead into **crm.toromovers.net** `/api/ingest`?
3. Durable Postgres for Pipeline CRM (Neon vs claim Prisma DB)?

---

## Credentials / access (no secrets in this file)

| System | Location |
|--------|----------|
| Main site Netlify | `live-toro-site` · toromovers.net |
| Pipeline Netlify | `toro-pipeline-crm` · crm.toromovers.net |
| n8n | `https://n8n-production-d3d0.up.railway.app` |
| HubSpot account | Mudanzas pipeline (IDs in `src/lib/hubspot.ts`) |
| Meta ads | act `971361825561389` · token in `toromovers-site/.env.wire` |
| Pipeline login | `admin@toromovers.net` / `admin123` (change in prod) |

OpenPhone / Resend keys live on **live-toro-site** Netlify env. Pipeline CRM proxies SMS via `/api/crm/send` + `LEAD_INTAKE_SECRET`.

---

## Artifacts (read these, don’t rewrite)

### Pipeline CRM
- App: `/Users/vynstudio/toro-pipeline`
- GitHub: https://github.com/vynstudio/toro-pipeline
- Deploy notes: `toro-pipeline/docs/DEPLOY.md`
- Live: https://crm.toromovers.net

### Main site automation
- Repo: `/Users/vynstudio/toromovers-site` (remote `live-toromovers`)
- Stage plan: `src/lib/crm/stage-sms-plan.ts`
- Runner: `src/lib/crm/run-stage-automation.ts`
- Docs: `docs/stage-sms-followups.md`, `docs/n8n-stage-sms-setup.md`, `docs/crm-automation.md`
- Workflow: `docs/n8n-stage-sms-drip.workflow.json`
- Plan JSON: https://toromovers.net/api/crm/sequences/plan

### Ads
- Fixed creatives: `~/Downloads/TORO-DILER/exports-fixed/`
- Targeting script: `~/Desktop/Toro-FullService-Ads/apply_targeting_35_65.py`
- Audit: `~/toromovers-ads-audit-2026-06-15.md`

---

## Next session checklist (n8n)

1. n8n → Import `docs/n8n-stage-sms-drip.workflow.json`
2. Activate workflow
3. Confirm Production URL =  
   `https://n8n-production-d3d0.up.railway.app/webhook/toro-stage-sms-drip`
4. Railway: `N8N_WEBHOOK_SECRET` + optional `LEAD_INTAKE_SECRET` match Netlify
5. Test:
   ```bash
   curl -X POST https://toromovers.net/api/crm/sequences/stage-run \
     -H "Content-Type: application/json" \
     -H "x-lead-secret: $LEAD_INTAKE_SECRET" \
     -d '{"stage":"newLead","stepId":"new_1h","firstName":"Test","phone":"+1…","consentSms":true}'
   ```
6. Test 1-minute delay via webhook with `delayMinutes: 1` (see setup doc)

---

## Skills for next agent

- Existing site patterns: read `docs/crm-automation.md`, `docs/wiring-runbook.md`
- Deploy main site: Netlify `live-toro-site`
- Do **not** mix Pipeline CRM secrets into main site without need
