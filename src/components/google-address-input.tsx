/**
 * Address autocomplete — Mapbox first, Google Places fallback.
 * Kept at this path so existing imports keep working.
 */
export {
  AddressAutocomplete,
  AddressAutocomplete as GoogleAddressInput,
} from "./address-autocomplete";
